machine:
  environment:
    PATH: $HOME/.cabal/bin:$HOME/installs:$PATH
dependencies:
  override:
    - sh ~/librato/install_deps.sh
  cache_directories:
    - "~/installs"
test:
  override:
    - echo $PATH
    - pwd
    - cd ~/librato
    - cabal install --only-dependencies
    - cabal configure
    - cabal build

